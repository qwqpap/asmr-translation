# ASMR 日语音频转中文 LRC：v0.1 历史 TODO

> 本文保留 v0.1 命令行版本的原始范围。v0.2 已新增上下文双阶段翻译、外部
> OpenAI-compatible 路由和原生 Win32 GUI；当前说明与验收以 README 和
> `docs/VALIDATION.md` 为准。

## 1. 项目目标

实现一个 Windows 本地命令行工具：输入包含 ASMR 音频的文件夹，批量识别其中的日语人声，调用本地 Ollama 翻译为简体中文，并在音频旁生成同名 `.lrc` 文件。

示例：

```text
D:\ASMR\耳かき.flac
D:\ASMR\耳かき.lrc
```

最终 LRC 只保留中文，不记录呼吸、敲击、摩擦等非语言声音：

```lrc
[00:03.25]今天辛苦了。
[00:08.70]我们先从右耳开始吧。
```

## 2. 已确认的运行环境与约束

- 操作系统：Windows。
- GPU：NVIDIA GeForce GTX 1660 Ti，6GB VRAM。
- Python：3.12.7。
- FFmpeg：已安装且可执行。
- Ollama：已安装且可执行。
- 当前可用 Ollama 模型：
  - `qwen3.5-9b-abliterated:latest`
  - `qwen3.5-0.8b:latest`
- 语音识别固定以日语为主，翻译目标固定为简体中文。
- 必须全程本地处理，不上传音频、转写文本或翻译内容。
- 默认一次只处理一个音频文件，不并行运行多个识别任务。
- Whisper 与 Ollama 不得同时占用 GPU。

## 3. 第一版范围

### 必须完成

- 接收一个文件夹路径。
- 递归扫描常见音频格式：`.mp3`、`.m4a`、`.flac`、`.wav`、`.opus`、`.ogg`、`.aac`。
- 使用本地 faster-whisper 识别日语人声并取得可靠的行级时间戳。
- 使用本地 Ollama 将日语字幕翻译成简体中文。
- 为每个音频生成同目录、同文件名的 `.lrc`。
- 保存可恢复的中间转写结果，支持中断后继续。
- 单个文件失败不得中断整个批次。
- 已存在的 LRC 默认跳过，只有显式指定 `--overwrite` 才能覆盖。
- 输出清楚的进度、失败原因和最终汇总。

### 第一版不做

- v0.1 不开发 GUI；v0.2 已交付原生 Win32 GUI。
- 不生成双语 LRC。
- 不输出呼吸、敲击、摩擦、环境声等声音标签。
- 不修改、重编码或覆盖原始音频。
- 不实现多人并行转写。
- 不依赖在线 API、云端模型或账号登录。
- 不把结构检查或单元测试通过等同于真实 ASMR 听感验收。

## 4. 强制架构准则

### 4.1 分阶段执行，避免显存争用

采用两个彼此隔离的阶段：

```text
扫描音频
  -> ASR 子进程加载 faster-whisper
  -> 完成转写并写入中间文件
  -> ASR 子进程彻底退出并释放显存
  -> 翻译阶段调用 Ollama
  -> 生成中文 LRC
```

- 不允许在 Whisper 模型仍驻留 GPU 时启动 Ollama 推理。
- ASR 阶段和翻译阶段之间必须有明确的磁盘缓存边界。
- 优先用进程退出释放 CUDA 资源，不依赖 `del model` 能完全释放显存。
- 如果 Ollama 已加载模型，开始 ASR 前应检测并给出可操作提示；不得静默进入显存不足状态。

### 4.2 识别模型策略

默认候选配置：

```text
model: large-v3
device: cuda
compute_type: int8_float16
language: ja
concurrency: 1
```

- `large-v3 + int8_float16` 是 6GB VRAM 环境的首选候选方案，但必须经过实机峰值显存和稳定性验证后才能标记为默认已验收配置。
- 遇到 CUDA OOM 时，应给出明确错误和降级建议，不得无限自动重试。
- 降级候选为 `medium + int8_float16`；是否自动降级应由显式配置控制，并在日志中记录。
- 不使用以英语为主要目标的蒸馏模型作为日语默认模型。
- 不做激进降噪；所有预处理只能作用于临时识别输入，不得改变原音频。

### 4.3 ASMR 耳语处理

- VAD 参数必须对轻声耳语保持宽松，不能直接沿用普通会议录音的激进阈值。
- 不能仅凭低置信度删除字幕，因为真实耳语本身可能置信度较低。
- 必须结合无语音概率、重复模式、时间区间和上下文过滤幻觉。
- 应检测并处理以下典型问题：
  - 长静音后凭空生成句子。
  - 连续重复同一句话。
  - 出现“感谢观看”“请订阅”等与内容无关的常见幻觉。
  - 将纯敲击或摩擦声识别成语言。
- 每一条被过滤的候选字幕必须在调试数据中保留原因，便于复核。
- 过滤规则应可配置、可测试，不把复杂规则散落在业务流程中。

### 4.4 翻译策略

- 不逐行孤立翻译；按连续上下文分组发送给 Ollama。
- 每条原文必须带稳定 ID，返回结果必须按 ID 重新绑定时间戳。
- 要求模型返回结构化 JSON，不直接解析自然语言说明。
- 必须校验：ID 数量、ID 唯一性、遗漏、重复、空译文和额外内容。
- 格式校验失败时允许有限次数重试；超过限制后保存错误并继续其他文件。
- 翻译采用低随机性，优先忠实、自然，不擅自扩写内容。
- 中文输出不得包含思考过程、解释、Markdown 或日文原文。
- 第一轮可测试现有 `qwen3.5-9b-abliterated:latest`，但代码不得与该模型名称绑定。
- 修改版模型必须通过格式稳定性和翻译质量对比后才能成为推荐默认值；必要时改用官方 Qwen 4B 至 8B 模型。

### 4.5 时间轴与 LRC 生成

- 使用识别得到的真实时间戳，不按文本长度凭空平均整段时间。
- 超长字幕优先依据停顿、标点和单词级时间戳切分。
- 相邻行时间戳必须单调递增。
- 空译文不得写入 LRC。
- 默认输出 UTF-8；需验证至少一个 Windows 常用播放器的中文兼容性，再决定是否默认添加 BOM。
- LRC 写入必须使用临时文件加原子替换，避免中断时留下半个成品。
- 未指定 `--overwrite` 时绝不覆盖已有 `.lrc`。

## 5. 数据与文件约定

建议使用独立缓存目录保存中间结果，不污染音频目录：

```text
<项目目录>/.cache/
  <音频内容或路径哈希>/
    source.json
    transcript.raw.json
    transcript.filtered.json
    translation.zh-CN.json
    process.log
```

中间数据至少记录：

- 原始音频绝对路径。
- 文件大小、修改时间和内容指纹。
- ASR 模型、计算类型和关键参数。
- 每个片段的起止时间、原文和置信度信息。
- 被过滤内容及过滤原因。
- Ollama 模型名称、翻译批次和结构校验结果。
- 工具版本和处理时间。

缓存有效性不能只依赖文件名；原文件发生变化时必须重新处理。

## 6. 命令行接口目标

基础用法：

```powershell
python -m asmr_lrc "D:\ASMR"
```

计划支持：

```powershell
python -m asmr_lrc "D:\ASMR" --transcribe-only
python -m asmr_lrc "D:\ASMR" --translate-only
python -m asmr_lrc "D:\ASMR" --overwrite
python -m asmr_lrc "D:\ASMR" --asr-model medium
python -m asmr_lrc "D:\ASMR" --ollama-model <model-name>
python -m asmr_lrc "D:\ASMR" --dry-run
```

命令行规范：

- 路径中包含空格、中文或日文时必须正常工作。
- `--dry-run` 只能报告将处理、跳过或覆盖哪些文件，不能写入结果。
- 参数错误返回非零退出码，并给出简洁可操作的说明。
- 批处理中部分文件失败时，最终退出状态和汇总必须明确区分完全成功、部分成功与完全失败。
- 日志不能包含无意义的模型内部输出或 Ollama 思考过程。

## 7. 编码规范

- 使用清晰的 Python 包结构，不把扫描、识别、过滤、翻译和 LRC 写入全部堆在单个脚本中。
- 新代码添加类型标注；面向外部的数据结构使用明确的 dataclass 或验证模型。
- 业务逻辑与 CLI、进度显示、文件系统操作解耦。
- 所有路径使用 `pathlib.Path`。
- 外部进程调用使用参数列表，不拼接 shell 命令字符串。
- 所有文本文件显式指定编码。
- 不吞掉异常；面向用户的错误与调试堆栈分开保存。
- 不使用裸 `except`。
- 不在源码中硬编码用户目录、模型缓存目录或 Ollama 地址。
- 配置必须有代码内默认值，并允许通过 CLI 覆盖；第一版不引入复杂配置系统。
- 依赖保持精简并固定兼容范围，不使用全局 Anaconda 环境作为可复现性保证。
- 建立项目专用虚拟环境，依赖安装说明必须可从干净环境复现。
- 修改前检查工作区状态；不覆盖或清理无关的用户文件。
- 每一阶段完成后运行针对性测试；提交验收前运行完整测试和静态检查。

建议模块边界：

```text
asmr_lrc/
  cli.py
  config.py
  scanner.py
  models.py
  asr.py
  filters.py
  translator.py
  lrc.py
  cache.py
  reporting.py
tests/
```

## 8. 待办任务

### M0：项目骨架与环境探测

- [ ] 创建 Python 项目骨架和专用虚拟环境说明。
- [ ] 添加最小依赖清单和版本约束。
- [ ] 实现 Python、FFmpeg、NVIDIA/CUDA、Ollama 环境探测。
- [ ] 检测 faster-whisper/CTranslate2 是否能使用 CUDA。
- [ ] 检测 Ollama 服务和指定模型是否可用。
- [ ] 实现 `--help` 和 `--dry-run`。
- [ ] 添加 Windows 中文、日文和空格路径测试。
- [ ] 记录一次 1660 Ti 6GB 环境的探测结果。

### M1：文件扫描与缓存

- [ ] 递归扫描受支持的音频格式。
- [ ] 统一并稳定排序待处理文件。
- [ ] 实现已有 LRC 跳过规则和 `--overwrite`。
- [ ] 实现音频指纹与缓存目录映射。
- [ ] 设计并验证中间 JSON 数据结构。
- [ ] 实现断点续跑和失效缓存识别。
- [ ] 保证缓存损坏时能报告并安全重建。

### M2：日语 ASR

- [ ] 集成 faster-whisper。
- [ ] 固定 `language=ja`，避免自动语言检测误判耳语。
- [ ] 验证 `large-v3 + int8_float16` 在 6GB VRAM 上能否稳定加载和运行。
- [ ] 测量峰值显存、处理耗时和实时倍率。
- [ ] 实现显存不足时的清晰诊断与可选降级。
- [ ] 保存完整原始片段、时间戳和置信度信息。
- [ ] 确保 ASR 独立进程退出后显存得到释放。

### M3：ASMR 幻觉过滤与分行

- [ ] 建立典型幻觉短语和重复模式检测。
- [ ] 结合无语音概率与时间信息过滤纯非语言区间。
- [ ] 实现可审计的过滤原因记录。
- [ ] 实现字幕合并、拆分和时间戳单调性校验。
- [ ] 使用真实耳语、敲击和长静音样本调参。
- [ ] 为每条过滤规则添加单元测试。
- [ ] 防止过滤器误删低置信度但真实存在的耳语。

### M4：Ollama 日中翻译

- [ ] 实现 Ollama 可用性与模型检查。
- [ ] 设计携带稳定 ID 的分组翻译协议。
- [ ] 实现 JSON 返回校验和有限重试。
- [ ] 保持译文与原始字幕一一对应。
- [ ] 实现批次大小限制，控制上下文和显存占用。
- [ ] 禁止输出思考过程、说明文字和日文原文。
- [ ] 对现有 9B 修改版模型进行质量与格式稳定性测试。
- [ ] 如有必要，对比一个官方 Qwen 4B 至 8B 模型。
- [ ] 翻译结束后确保 Ollama 模型可按配置释放。

### M5：LRC 输出与批处理容错

- [ ] 生成纯中文标准 LRC。
- [ ] 使用临时文件和原子替换安全写入。
- [ ] 验证中文、日文、空格及超长文件名。
- [ ] 验证时间戳格式、顺序、空行和重复行。
- [ ] 单文件失败后继续处理剩余文件。
- [ ] 输出成功、跳过、缓存命中和失败数量汇总。
- [ ] 验证至少一个实际 Windows 播放器的中文 LRC 兼容性。

### M6：真实音频验收

- [ ] 准备至少五类本地样本：极轻耳语、普通轻声、大量敲击、长静音、长音频。
- [ ] 每类样本人工抽听并对照原始时间轴。
- [ ] 记录漏识别、幻觉、时间偏移和翻译问题。
- [ ] 比较 `large-v3` 与必要的降级模型，不只比较运行速度。
- [ ] 确认整个批次不会同时加载 Whisper 和 Ollama。
- [ ] 确认中断后能够从已有转写或翻译缓存继续。
- [ ] 在真实文件夹上完成一次端到端批处理。
- [ ] 将结构测试、自动化测试、性能测试和人工听感验收分别报告。

### M7：文档与交付

- [ ] 编写安装、模型准备和首次运行说明。
- [ ] 说明 CUDA 不可用、显存不足、Ollama 未启动等常见问题。
- [ ] 提供不覆盖已有 LRC 的安全说明。
- [ ] 记录已验证的模型、参数、GPU、驱动和依赖版本。
- [ ] 给出完整卸载或清理缓存方法，但不自动删除用户数据。

## 9. 第一版验收标准

只有同时满足以下条件，才可称为第一版完成：

- 能从指定文件夹递归处理受支持的音频。
- 在 GTX 1660 Ti 6GB 上稳定完成至少一次 `large-v3` 或明确记录并采用经验证的降级配置。
- Whisper 和 Ollama 不同时驻留 GPU。
- 对每个成功音频生成可被实际播放器读取的同名纯中文 LRC。
- 不覆盖原音频；没有 `--overwrite` 时不覆盖已有 LRC。
- 长静音和纯敲击区间不存在大段明显幻觉字幕。
- 中断后能利用缓存继续，而不是从头处理全部文件。
- 单个损坏音频不会终止整个批次。
- 自动化测试通过，且真实 ASMR 样本完成抽听复核。
- 明确报告仍存在的漏识别、时间偏差和翻译局限，不用“命令成功”代替质量验收。

## 10. 后续可选项

以下内容只有在命令行版本完成并通过真实音频验收后再考虑：

- [v0.2 已完成] Windows 拖放式 GUI。
- [v0.2 已完成] 双语字幕预览、试听、跳转和中文手动编辑。
- 右键菜单集成。
- 可选原文或双语 LRC。
- 多种目标语言。
- CPU-only 模式。
- 更专业的日语专用 ASR 模型对比。
